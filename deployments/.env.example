POSTGRES_USER=ace
POSTGRES_PASSWORD=acepass
POSTGRES_DB=aceplat

# API
API_TOKEN=changeme
API_PORT=8081

# Health checker
CHECK_INTERVAL=10s
CHECK_TIMEOUT=2s
CHECK_CONCURRENCY=16

# Caddy sync
CADDYFILE_PATH=/out/Caddyfile
CADDY_ADMIN_URL=http://caddy:2019/load
CADDY_DEPLOY_MODE=local        # local | ssh
CADDY_OUT_VOLUME=/out          # mount target inside container
CADDY_SSH_HOST=vps.example.com
CADDY_SSH_USER=root
CADDY_SSH_PATH=/etc/caddy/Caddyfile
DRY_RUN=false
